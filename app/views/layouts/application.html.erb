<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= content_for?(:title) ? yield(:title) : "Trivia" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Trivia" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
    <%= stylesheet_link_tag href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" %>
    <%= csrf_meta_tags %>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.js" type="text/javascript"></script>
  </head>
  <body>
  <header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <div class="col-sm-2">
      <%= link_to 'Home', root_path ,class: 'navbar-brand'%>
    </div>
      <nav class="col-sm-10">
        <ul class="nav navbar-nav navbar-right">
          <% if current_user %>
            <span class="navbar-text">
              <strong><%= current_user.name %></strong>!
            </span>
            <li><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
          <% else %>
            <li><%= link_to "Sign in", "/auth/facebook", id: "sign_in" %></li>
          <%end%>
        </ul>
      </nav>
    </div>
  </header>
  <div class="row">
    <div class="col-md-12">
      <main role="main">
        <%= render 'layouts/messages' %>
        <%= yield %>
      </main>
    </div>
  </div>
  </body>
</html>
